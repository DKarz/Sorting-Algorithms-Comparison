#include "func.h"
#include <iostream>
#include <list>
#include <vector>
#include <ctime>
#include <algorithm>
#include <fstream>

#include "InsertionSort.h"
#include "MergeSort.h"
#include "QuickSort.h"
#include "RadixSort .h"
#include "CountingSort.h"

using namespace std;


/// All the commented code was used for testing

int copy1;
list<int> copy2;
string copy3;
list<string> copy4;
vector<int> copy5;
vector<string> copy6;
int n, m;
vector<time_t> insert;
vector<time_t> merget;
vector<time_t> quick;
vector<time_t> counts;
vector<time_t> radix;




//void record(string name)
//{
//    ofstream file(name);
//    for (int k = 0; k < insert.size(); ++k)
//    {
//        file << k + n << ";" << insert[k] << ";" << merget[k] << ";" << quick[k] << "\n";
//    }
//}

int main()
{

//    cin >> n >> m;
//    //setlocale(LC_ALL, "ru");
//    srand(time(NULL));
//    //int const SIZE = 8;
//    int arrInt[n];
//    string arrStr[n];
//    list<int> listInts;
//    list<string> listStr;
//
//
//    for (int i = n; i < m; ++i)
//    {
//        string str("");
//        for (int z = 0; z < rand() % 20; ++z)
//            str+= (rand()%10 + charCap());
//        vecOfStrings.push_back(str);
//    }
//    for (auto el : vecOfStrings) cout << el << ", ";


    return 0;
}